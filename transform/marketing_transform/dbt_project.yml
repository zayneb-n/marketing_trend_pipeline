name: marketing_transform
version: '1.0.0'
config-version: 2

profile: marketing_transform

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  marketing_transform:
    staging:
      +materialized: view
    marts:
      +materialized: table
